const searchMinHeight = (current) => {

    let minHeight = 0;

    if (current > minHeight) {
      minHeight = current
      console.log('current :>> ', current);
      console.log('minHeight :>> ', minHeight);
    }
    return minHeight
}
  
export default searchMinHeight